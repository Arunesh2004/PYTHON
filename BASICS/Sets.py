numbers = {1, 1, 1, 2, 4, 5, 3, 2}

print("Original set: ", numbers) # print the original set i.e the numbers that were repeated are printed only once.

# sets doesn't have indexes (Thats why they are called unordered). therefore to iterate over sets we use loops

for num in numbers:
    print(num)